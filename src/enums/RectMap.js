import { baseModel, titleModel } from "./PrevDataModel"

const rectMap = {
  bar: {
    base: baseModel,
    chart: {
      title: titleModel
    }
    
  },
  line: {
    base: baseModel,
    chart: {
      title: titleModel
    }
  }
}

export default rectMap